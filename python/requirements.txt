# =====================================================================
# DLC live-inference (LEAN) environment — pip-installable requirements
# Python 3.10.11 (cp310, win_amd64)
# Generated from requirements.lock.txt (verified against pip freeze).
#
# !!! TORCH IS NOT INSTALLED FROM THIS FILE !!!
# torch / torchvision / torchaudio are the +cu128 builds and live ONLY on
# the PyTorch CUDA 12.8 wheel index, NOT on PyPI. Install them FIRST and
# SEPARATELY (do NOT put --index-url on this file, it would break the rest):
#
#   pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 torchaudio==2.10.0+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Then install everything below from normal PyPI:
#   pip install -r requirements.txt
#
# The +cu128 wheels BUNDLE the CUDA 12.8 runtime + cuDNN 9.10. No CUDA
# Toolkit and no nvidia-* pip packages are needed to RUN torch.
# =====================================================================

# --- Top-level direct dependencies (what the live bridge actually imports) ---
deeplabcut-live==1.1.0
numpy==1.26.4
opencv-python==4.11.0.86
scipy==1.15.3
pandas==2.3.3
pillow==12.1.1
PyYAML==6.0.3
ruamel.yaml==0.19.1
tables==3.10.1
timm==1.0.26
huggingface_hub==1.9.0
networkx==3.4.2

# --- Pinned transitive deps (pinned for an exact, conflict-free rebuild) ---
# Pulled in by deeplabcut-live / timm / huggingface_hub / torch. Pinning them
# guarantees the resolver reproduces the source machine bit-for-bit.
annotated-doc==0.0.4
anyio==4.13.0
blosc2==4.1.2
certifi==2026.2.25
charset-normalizer==3.4.7
click==8.3.2
colorama==0.4.6
colorcet==3.1.0
dlclibrary==0.0.11
einops==0.8.2
exceptiongroup==1.3.1
filelock==3.25.2
fsspec==2026.2.0
h11==0.16.0
hf-xet==1.4.3
httpcore==1.0.9
httpx==0.28.1
idna==3.11
Jinja2==3.1.6
markdown-it-py==4.0.0
MarkupSafe==3.0.3
mpmath==1.3.0
msgpack==1.1.2
ndindex==1.10.1
numexpr==2.14.1
packaging==26.0
py-cpuinfo==9.0.0
Pygments==2.20.0
python-dateutil==2.9.0.post0
pytz==2026.1.post1
requests==2.33.1
rich==14.3.3
safetensors==0.7.0
shellingham==1.5.4
six==1.17.0
sympy==1.14.0
tqdm==4.67.3
typer==0.24.1
typing_extensions==4.15.0
tzdata==2026.1
urllib3==2.6.3

# =====================================================================
# NOT pip-installable / intentionally omitted:
#   * torch / torchvision / torchaudio  -> installed separately (see header)
#   * gxipy                             -> NOT a pip package. It ships inside
#       the Daheng Galaxy SDK ("Samples/Python SDK"); the code adds it to
#       sys.path at runtime (rt_dlc_live._prepare_sdk_environment). Install
#       the Galaxy SDK, do NOT pip install gxipy.
#   * full 'deeplabcut' (training/analyze) -> NOT in this env. This is the
#       LEAN LIVE env. Only add it if you need offline run_dlc.py
#       (train / analyze). See the migration guide.
# =====================================================================
