# Минимум, на котором проходит набор тестов.
#
# Это НЕ боевое окружение (см. requirements.txt): ни torch, ни deeplabcut-live,
# ни Galaxy SDK тестам не нужны - данные синтетические, GPU не нужен. Смысл
# отдельного файла в том, чтобы проверка на гитхабе ставилась за полминуты и не
# зависела от колёс CUDA, которых на PyPI всё равно нет.
#
# Версии совпадают с боевыми, чтобы проверка ловила то же поведение.
numpy==1.26.4
pandas==2.3.3
# headless: на раннере нет иксов, а GUI-функции cv2 в тестах не вызываются.
# Модуль всё равно называется cv2, импорты в коде менять не надо.
opencv-python-headless==4.11.0.86
pytest==8.4.2
